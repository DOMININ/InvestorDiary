import { createStyles, makeStyles, Theme } from "@material-ui/core/styles";

const usePieChartSimpleStyles = makeStyles((theme: Theme) =>
  createStyles({
    pieChart: {
      margin: "0 auto",
    },
  })
);

export default usePieChartSimpleStyles;
