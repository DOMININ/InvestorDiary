import { makeStyles } from "@material-ui/core";

const useProfitAddStyles = makeStyles(() => ({
  paper: {
    display: "flex",
    flexDirection: "column",
    marginBottom: 30,
    paddingTop: 20,
    paddingBottom: 20,
  },
  title: {
    marginBottom: 15,
  },
  margin: {
    marginRight: 20,
  },
}));

export default useProfitAddStyles;
